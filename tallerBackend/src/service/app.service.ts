/* import {repository} from '@loopback/repository';
import {PropietarioRepository} from '../repositories/propietario.repository';

export class Utils {
  constructor(
    @repository(PropietarioRepository)
    public PropietarioRepository : PropietarioRepository,
  ) {}

     async validationUserId(propietarioId): Promise<Propietario>{
      const isUser= await PropietarioRepository.findById(propietarioId);
      return this.PropietarioRepository.findById(propietarioId)
    }
    if (condition) {

    } else {

    }


  }
 */
